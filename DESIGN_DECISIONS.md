# 設計に関する意思決定の記録 (Architecture Decision Records)

このドキュメントは、本プロジェクトにおける設計や技術面での様々な意思決定に関する記録です。

---

**メッセージ機能のバックエンド処理を行うAPIを、LaravelではなくGoで用意した理由**
- パフォーマンスの最大化によるユーザー体験の向上
    - 今回SPAとして実装したメッセージ機能は、画面上でのユーザー操作が頻繁に発生することを想定している。そのため、APIの高速なレスポンスがユーザー体験の質に直結すると考えた。
    - Goは事前にコンパイルされる言語であり、リクエストごとにコードを解釈するPHP（Laravel）よりも実行速度に優れている。このパフォーマンスの優位性を活かし、より快適な操作感を提供することを第一の目的としている。
- モダンな技術スタックへの挑戦
    - LaravelのバックエンドにAPIを実装する方がシンプルであることは承知の上で、あえてGoを採用することで、新しい技術への挑戦とスキルセットの向上を目指した。
    - パフォーマンスが求められるAPI部分をGoで切り出すという構成は、将来的なマイクロサービス化など、よりモダンなアーキテクチャへの発展を見据えた知見を得る良い機会だと判断した。

<br>

**トークン生成に使用する秘密鍵をローテーションさせず固定して使用している理由**
- トークン生成には、追加のランダム性（ランダムな文字列とnonce）があるため、鍵単体では有効なトークンを生成される可能性が低い
- アクセストークンとリフレッシュトークンの両方に短い有効期限を設定している
- セキュリティ上の懸念より、秘密鍵の管理負荷を軽減できるというメリットの方が大きいと考えたため

<br>

**API Gatewayを導入しない理由**
- API Gatewayの主な利用目的との不一致
    - API Gatewayは、主に「外部クライアントへのAPI公開」「認証・認可・レート制限等の付加機能」「サーバーレス(Lambda)連携」といった要件で大きな効果を発揮する
    - 一方、本システムのGo APIはVPC内の内部通信に限定されるため、これらの要件に該当しない
- 外部へのAPI公開が不要であること
    - Go APIは外部クライアント向けではなく、同一VPC内のサービス（ECS Fargateタスク、Lambda）からのアクセスに限定される
    - このため、インターネットとの境界に立ち、APIを保護・公開するAPI Gatewayを介すメリットが薄い
- 高度な制御機能が不要であること
    - 本システムでは、API Gatewayが提供する以下の様な高度な機能は要求されない
        - API呼び出しの回数制限、モニタリング、課金
        - 複数バージョンのAPIやマイクロサービスの統合管理
        - APIキーによる認証、Cognito連携による認可、Usage Planによるレート制限
- コストと運用負荷の増加
    - API Gatewayを導入すると、設定・管理対象のコンポーネントが増え、アーキテクチャが複雑化する
    - APIコール数に応じた追加の従量課金も発生する
- 既存コンポーネントで要件を充足可能であること
    - VPC内のサービス間通信は、内部向けALBとセキュリティグループで実現できる
    - これにより、シンプルかつセキュアなアクセス制御を、追加コストを抑えながら満たすことが可能である

<br>

**CPUとリクエスト数に基づくオートスケーリング戦略を採用した理由**
- 持続的な負荷に対応するCPUベースのスケーリング
    - 役割：長時間続く負荷の変動に対応し、リソースを安定的に維持する
    - 背景：Go言語の特性上、CPU使用率が負荷のバロメーターとして機能しやすいため、CPUを指標に採用
- 突発的なトラフィックに対応するリクエストベースのスケーリング
    - 役割：短時間の急激なアクセススパイクを検知し、迅速にスケールアウトする
    - 背景：CPU使用率だけでは、急激なリクエスト増に即座に追従できない場合があるため、ALBの1タスクあたりのリクエスト数を指標として併用
- 2つの戦略を組み合わせるメリット
    - 役割分担：CPU指標が「持続的な負荷の安定化」を、リクエスト数指標が「突発的なバーストへの対応」を担うことで、異なる種類の負荷変動に両対応できる
    - 可用性とコストの両立：安定時はCPUベースでリソースを最適化しつつ、スパイク時のみリクエスト数ベースで迅速にスケールするため、コスト効率と可用性を両立できる

<br>

**インテグレーションテストを導入せずユニットテストのみに限定した理由**
- インテグレーションテスト導入の課題（DBスキーマの同期問題）
    - DBスキーマはLaravelのリポジトリでマイグレーション機能を用いて管理されている
    - GoのCI環境でインテグレーションテストを行うには、このスキーマを同期する仕組み（例: LaravelのマイグレーションをCIで実行する）が必要となり、設定が複雑化する
    - 将来Laravel側でスキーマ変更がある度に、Goのテスト環境にも変更を反映させる必要があり、リポジトリ間の依存と運用コストが増大する
- ユニットテストのみで対応するメリット
    - CIのシンプル化：データベースのモックを利用するため、CI環境で実DBを準備・起動する必要がない
    - 責務の分離：DBスキーマ管理の責務をLaravelリポジトリに集中させ、Goのテストがスキーマ変更に影響されないようにできる
- リスク評価と最終判断
    - 低いリスク：APIの機能が基本的なCRUD処理に留まり、複雑なSQLクエリも存在しないため、実DBでテストしないことによるリスクは低い
    - テスト対象の切り分け：クレデンシャル生成などの主要ロジックはDBアクセスよりもアルゴリズム部分のテストが重要であり、これはモックを用いたユニットテストで十分に検証可能である
    - コストと恩恵のバランス：DBスキーマの二重管理といった導入・運用コストに対し、インテグレーションテストによって得られる恩恵が現時点では見合わない
- 結論
    - プロジェクトの規模とリスクを考慮し、現段階ではモックベースのユニットテストのみを導入する
    - 将来、APIの機能が拡張され、より厳密なDB結合テストの必要性が高まった際に、インテグレーションテストの導入を検討する方針とする
